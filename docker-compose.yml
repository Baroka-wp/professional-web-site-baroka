services:
  app:
    build: .
    ports:
      - "${PORT_HOST:-3001}:3000"
    environment:
      - NODE_ENV=production
      - DATABASE_URL=${DATABASE_URL}
      - BREVO_API_KEY=${BREVO_API_KEY}
      - GEMINI_API_KEY=${GEMINI_API_KEY}
    restart: always
